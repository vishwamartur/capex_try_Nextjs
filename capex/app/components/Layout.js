// app/components/Layout.js

import React from "react";

const Layout = ({ children }) => {
  return (
    <div>
      {/* You can include common layout elements here */}
      {children}
    </div>
  );
};

export default Layout;
